{
  "name": "@prose-motions/styles",
  "version": "0.1.0",
  "private": true,
  "description": "CSS for prose-motions — caret shapes and visual-mode styling.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./dist/vim.css",
    "./vim.css": "./dist/vim.css"
  },
  "main": "./dist/vim.css",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "bun run clean && mkdir -p dist && cp -R src/* dist/"
  },
  "files": [
    "dist/**/*"
  ]
}
